# ==============================================================================
# Odoo Public — Production Overlay
# ==============================================================================
# Fill every "" value from `terraform output` after `terraform apply`.
# ==============================================================================

instance: "public"

image:
  repository: ""      # TODO: terraform output -raw ecr_odoo_repository_url
  tag: "latest"

service:
  nodePort: 30080

db:
  host: ""            # TODO: terraform output -raw odoo_rds_endpoint  (same RDS as private)
  name: ""            # TODO: terraform output -raw odoo_db_name
  user: ""            # TODO: terraform output -raw odoo_db_username

persistence:
  storageClassName: "efs-static-odoo-public"
  efsId: ""           # TODO: terraform output -raw efs_id
  accessPointId: ""   # TODO: terraform output -raw efs_odoo_public_access_point_id

serviceAccount:
  irsaRoleArn: ""     # TODO: terraform output -raw odoo_public_irsa_role_arn

secrets:
  dbPasswordSecretId: ""  # TODO: terraform output -raw odoo_db_password_secret_id
