apiVersion: v1
kind: Service
metadata:
  name: odoo
  namespace: odoo-private
spec:
  type: NodePort
  selector:
    app: odoo-private-gateway
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30081
      protocol: TCP
